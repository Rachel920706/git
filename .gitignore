# 애스터리스크(*)는 문자가 한도 없거나 하나 이상을 의미
# [abc]는 중괄호 안에 있는 문자 중 하나를 의미
# 물음표(?)는 문자 하나를 의미
# [0-9] 붙임표(-)는 그 캐릭터 사이에 있는 문자 하나를 의미
# 애스터리스크 2개(**)로 디렉터리 안의 디렉터리를 의미
#  ex) a/**/z -> a/z, a/b/z, a/b/c/z 디렉터리에 사용할 수 있음을 의미

# 확장자가 .o or .a 인 파일 무시
*.[oa]

# 확장자가 ~ 인 파일 무시 
*~

# 확장자가 .a인 파일 무시
*.a

# 윗 라인에서 확장자가 .a인 파일은 무시하게 했지만 lib.a는 무시하지 않음
!lib.a

#현재 디렉터리에 있는 TODO 파일은 무시하고 subdir/TODO처럼 하위 디렉터리에 있는 파일은 무시하지 않음
/TODO

# build/ 디렉터리에 있는 모든 파일은 무시 
build/

# doc/notes.txt 파일은 무시하고 doc/server/arch.txt 파일은 무시하지 않음
doc/*.txt

# doc 디렉터리 아래의 모든 .pdf 파일을 무시
doc/**/.pdf
